<!DOCTYPE html>
<html lang="en" ng-app="APP">
<head>
    <meta charset="UTF-8">
    <title>RippaInc Selection Processs</title>
</head>

<body>
        <form method='post' action='http://localhost:3000/upload' enctype="multipart/form-data" name="uploadForm">
	        <input type='file' name='fileUploaded'>
	        <input type='submit' value="Upload" />
        </form>
        <hr>
		<form method='post' action='http://localhost:3000/input' name="inputForm">
		    <input type="text" name="userWebInput" placeholder="Place your input here" />
			<br>
		    <input type="submit" value="Envoyer" />
		</form>
		<% if(outputData.length > 0) { %>
			<strong>Output</strong>
			<% for( var i=0 ; i < outputData.length; i++ ) { %>
			   <p><%= outputData[i] %></p>
			<% } %>
		<% } else { %>
			<p><%= outputData %></p>
		<% } %>
 </body>
</html>